all:
  vars:
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python3

  hosts:
    node1<dns>:
      ansible_host: node1<host ip>

  children:
    grafana_server:
      hosts:
        node1<dns>:

